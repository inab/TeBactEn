{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EvidenciaDominio creation</h1>

    <form action="{{ path('evidenceDominium_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
{% endblock %}
